<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <%- include('./partials/header') %>

    <main class="">
      <%- include('./partials/aside') %>

      <div class="basis-12/12  min-h-screen w-full p-10"style="background-color:black;">
        <div class="items-center mb-10">
          <h2 class="text-white text-2xl font-bold"style="font-family:cursive;text-align:center">Your all posts are here..!!</h2>
        </div>

        <div class="mt-20">
          <h3 class="text-zinc-400 text-xl mb-5">Your Posts</h3>

          <div class="postcontainer">
            <% myPosts.reverse().map(post=>{ %>
            <div class="card rounded-md p-3 mb-2 w-1/3"style="background-color:black;box-shadow:inset 0px 0px 10px 0px rgb(115, 112, 112);padding:20px;">
              <img class="inline-block w-full h-80" src="<%= post.image %>" alt="img" />
              <div class="flex justify-between item-center mt-3">
                <h2 class="text-zinc-300 mb-1 text-xl">Title : <%= post.title %></h2>
                <h4 class="text-zinc-500 mb-1 text-sm">@ <%= user.name %></h4>
              </div>
              <p class="text-zinc-400 text-sm"><%= post.content %></p>
              
              <div class="flex justify-between items-center">
                <div class=""style="margin-top:20px;margin-bottom:11px;">
                  
                  <a class="text-white me-5 rounded-md" style="box-shadow:inset 0px 0px 30px 0px rgb(51, 109, 151);padding:10px 30px;" href="/editPost/<%= post._id %>">Edit</a>
                  <a
                    class="text-red-500 rounded-md" style="box-shadow:inset 0px 0px 30px 0px rgba(255, 0, 0, 0.669);padding:10px 30px;"
                    onclick="confirmBlogDelete(event)"
                    href="/deletePost/<%= post._id %>"
                    >delete</a
                  >
                </div>
                <span class="text-zinc-400 inline-block"
                  ><%= new Date(post.date).toLocaleString('en-US', { month: 'short', day: '2-digit',
                  year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span
                >
              </div>
            </div>

            <% }) %>
          </div>
        </div>
      </div>
    </main>

    <script src="../public/javascripts/deleteBlog.js"></script>
  </body>
</html>
